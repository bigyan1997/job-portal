{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m pip install -r requirements.txt && python3 manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate && gunicorn core.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "restartPolicyType": "ON_FAILURE"
  }
}
